---
const { posts } = Astro.props;
---

<div class="blog-grid">
  {posts.map(post => (
    <a href={post.slug} class="blog-card" target="_blank">
      <h2>{post.title}</h2>
      <p>{post.description}</p>
      <small>{new Date(post.date).toLocaleDateString()}</small>
    </a>
  ))}
</div>
